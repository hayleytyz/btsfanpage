{
  "name": "bts-fanpage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "@vercel/static-build": "^1.3.47"
  }
}
