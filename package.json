{
  "name": "bts-fanpage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "@vercel/static-build": "^1.3.47"
  }
}
