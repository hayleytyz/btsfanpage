{
  "name": "bts-fanpage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "workspaces": [
    "client"
  ],
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ]
}
